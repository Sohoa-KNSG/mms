---
title: "Đặc tả QC-05 - Tra cứu và hiệu chỉnh lịch sử QC"
use_case_id: "QC-05"
version: "1.0"
date: "2026-08-13"
status: "Đặc tả theo hồ sơ và contract mã nguồn hiện tại"
format: "Markdown - nguồn giao tiếp chuẩn"
---

# QC-05 – Tra cứu và hiệu chỉnh lịch sử QC

> Tài liệu thuộc bộ đặc tả 42 use case MMS. Nguồn sự thật gồm hồ sơ tổng thể, React route, .NET endpoint và SQL contract versioned trong workspace.

## Thông tin kiểm soát

| Thuộc tính | Giá trị |
| --- | --- |
| Module | Kiểm soát chất lượng |
| Wave | W2 |
| Tác nhân | QC, quản lý |
| Loại xử lý | Query + Command |
| Route React | `/quality/history` |
| Màn hình quyền | `scr_qc_log_phieu_kiem`, `scr_qc_log_phieu_nhanhang`, `scr_qc_log_info_edit` |
| Trạng thái | Contract đã có trong workspace; triển khai/UAT theo checklist chung |

---

## 1. Business Logic (Logic nghiệp vụ)

### 1.1. Mục tiêu

Theo dõi phiếu kiểm và sửa thông tin được phép.

### 1.2. Điều kiện trước và sau

| Loại | Nội dung |
| --- | --- |
| Điều kiện trước | Có quyền xem/sửa lịch sử. |
| Thành công | Hoàn tất đúng luồng 'QC-05', trả dữ liệu/kết quả contract và ghi audit khi có command. |
| Thất bại | Không để dữ liệu dở dang; trả lỗi nghiệp vụ hoặc 'traceId'. |

### 1.3. Luồng chính

1. Lọc phiếu.
2. mở chi tiết.
3. cập nhật trường được phép.
4. lưu audit..

### 1.4. Ngoại lệ và kiểm soát

Phiếu đã khóa: chỉ cho xem.

### 1.5. Business Rules

| Mã | Quy tắc |
| --- | --- |
| BR-QC-05-01 | User phải có phiên xác thực và quyền màn hình tương ứng. |
| BR-QC-05-02 | Dữ liệu bắt buộc phải được trim và kiểm tra tại API lẫn stored procedure. |
| BR-QC-05-03 | Không tin 'UserId', trạng thái hoặc giá trị suy diễn do client tự gửi. |
| BR-QC-05-04 | Stored procedure là nơi thực thi logic nghiệp vụ và phân quyền dữ liệu. |
| BR-QC-05-05 | Command phải nguyên tử, rollback khi bất kỳ bước nào lỗi. |
| BR-QC-05-06 | Giữ nguyên cấu trúc bảng và mã trạng thái legacy. |
| BR-QC-05-07 | Lỗi nghiệp vụ phải dùng mã ổn định; lỗi ngoài dự kiến phải có 'traceId'. |
| BR-QC-05-08 | React không truy cập bảng SQL trực tiếp. |

### 1.6. Ranh giới trách nhiệm

| Lớp | Trách nhiệm |
| --- | --- |
| React | Hiển thị, nhập liệu, validation trải nghiệm và trạng thái request |
| .NET API | Xác thực, contract HTTP, user claim, gọi SP và ánh xạ lỗi |
| SQL SP | Quyền, business rule, concurrency, transaction và dữ liệu |
| Legacy tables | Lưu dữ liệu/trạng thái vật lý hiện hành |

---

## 2. UI/UX Guidelines

### 2.1. Bố cục

- Header hiển thị mã 'QC-05' và tên “Tra cứu và hiệu chỉnh lịch sử QC”.
- Khu vực lọc/tìm kiếm hoặc form dữ liệu theo luồng nghiệp vụ.
- Vùng kết quả dạng bảng/chi tiết, có loading, empty, error và success state.
- Command quan trọng phải có xác nhận và khóa nút trong lúc gửi.

### 2.2. Trạng thái giao diện

| Trạng thái | Yêu cầu |
| --- | --- |
| Loading | Không hiển thị dữ liệu cũ như kết quả mới |
| Empty | Giải thích không có dữ liệu phù hợp |
| Validation | Gắn lỗi với đúng trường/dòng |
| Submitting | Chặn submit lặp; không tự retry command |
| Success | Hiển thị mã đối tượng, trạng thái và bước tiếp theo |
| Error | Thông báo thân thiện, nút thử lại và 'traceId' nếu có |

### 2.3. Accessibility

- Label rõ ràng cho input/select và nút chỉ có biểu tượng.
- Thao tác được bằng bàn phím; focus tới lỗi đầu tiên.
- Không dùng màu làm tín hiệu duy nhất.
- Thông báo dùng vùng 'aria-live'.

---

## 3. Programming Logic

### 3.1. React và route

| Screen | Route |
| --- | --- |
| scr_qc_log_phieu_kiem | /quality/history |
| scr_qc_log_phieu_nhanhang | /quality/history |
| scr_qc_log_info_edit | /quality/history |

### 3.2. .NET endpoint contract

| Endpoint name | Vai trò |
| --- | --- |
| `QC-05_GetInspectionHistory` | .NET endpoint đã định danh |
| `QC-05_UpdateInspectionResult` | .NET endpoint đã định danh |

Nguồn endpoint: apps/api/Modules/Quality/QualityEndpoints.cs.

### 3.3. Stored procedure contract

| Stored procedure | File nguồn |
| --- | --- |
| `api.usp_QC_QC05_GetInspectionHistory_v1` | `database/stored-procedures/w2/api.usp_QC_QC05_GetInspectionHistory_v1.sql` |
| `api.usp_QC_QC05_UpdateInspectionResult_v1` | `database/stored-procedures/w2/api.usp_QC_QC05_UpdateInspectionResult_v1.sql` |

**api.usp_QC_QC05_GetInspectionHistory_v1**

~~~sql
api.usp_QC_QC05_GetInspectionHistory_v1 @UserId nvarchar(50), @Search nvarchar(200) = NULL, @InspectionId int = NULL, @Page int = 1, @PageSize int = 50
~~~

**api.usp_QC_QC05_UpdateInspectionResult_v1**

~~~sql
api.usp_QC_QC05_UpdateInspectionResult_v1 @UserId nvarchar(50), @QcResultId int, @InspectionType nvarchar(50), @InspectedQuantity decimal(19,4), @FailedQuantity decimal(19,4), @ResultCode nvarchar(50), @OverallResultCode nvarchar(50), @DefectNote nvarchar(max) = NULL, @ExpectedChangedAt datetime2(7)
~~~

### 3.4. Error contract

| SQL number | HTTP | Ý nghĩa |
| --- | --- | --- |
| 51001 | 403 | Không có quyền xem lịch sử QC. |
| 51001 | 403 | Không có quyền hiệu chỉnh lịch sử QC. |
| 51022 | 422 | Loại kiểm chỉ nhận AQL hoặc 100%. |
| 51022 | 422 | Kết quả tiêu chí không hợp lệ. |
| 51022 | 422 | Kết luận vật tư không hợp lệ. |
| 51022 | 422 | Số lượng kiểm/không đạt không hợp lệ. |
| 51022 | 422 | ExpectedChangedAt là bắt buộc khi hiệu chỉnh. |
| 51009 | 409 | Kết quả không tồn tại hoặc phiếu đã khóa. |
| 51009 | 409 | Kết quả QC đã được cập nhật. Hãy tải lại dữ liệu. |
| 51022 | 422 | Kết luận Đạt mâu thuẫn với kết quả chi tiết. |
| 51022 | 422 | Kết luận Không Đạt cần ít nhất một lỗi hoặc số lượng không đạt. |
| Khác | 500 | Lỗi hệ thống; không lộ chi tiết nhạy cảm, bắt buộc có 'traceId' |

### 3.5. Concurrency và idempotency

- Command phải chạy trong transaction với XACT_ABORT ON.
- Cập nhật trạng thái cần expected state/time hoặc khóa phù hợp.
- Client không tự retry POST/PUT khi chưa có idempotency key.
- Retry không được tạo giao dịch trùng.

---

## 4. Data Logic

### 4.1. Ma trận dữ liệu

| Object | Vai trò | Truy cập qua |
| --- | --- | --- |
| `tbl_qc_phieu_kiem, tbl_qc_kiem, vw_ketqua_phieu_qc` | Dữ liệu nghiệp vụ legacy | SP |
| `api.vw_SEC_UserScreenAccess_v1` | Contract API/view/type | SP |
| `dbo.tbl_qc_phieu_kiem` | Dữ liệu nghiệp vụ legacy | SP |
| `dbo.tbl_phieu_nhan_hang` | Dữ liệu nghiệp vụ legacy | SP |
| `dbo.tbl_qc_kiem` | Dữ liệu nghiệp vụ legacy | SP |
| `dbo.tbl_chitiet_nhanhang` | Dữ liệu nghiệp vụ legacy | SP |
| `dbo.tbl_dm_vattu` | Dữ liệu nghiệp vụ legacy | SP |
| `dbo.tbl_tieuchi_kiem` | Dữ liệu nghiệp vụ legacy | SP |
| `dbo.tbl_his_phieunhap` | Dữ liệu nghiệp vụ legacy | SP |

### 4.2. Nguyên tắc dữ liệu

- Không thay đổi 59 bảng legacy hoặc mã trạng thái hiện hữu trong use case này.
- Mọi truy cập từ ứng dụng đi qua schema 'api' và stored procedure versioned.
- User/audit lấy từ phiên xác thực.
- Tất cả ghi nghiệp vụ liên quan phải cùng commit hoặc cùng rollback.

### 4.3. State model

~~~text
[Chưa đủ điều kiện]
        |
        | kiểm tra quyền + business rules
        v
[Sẵn sàng xử lý QC-05]
        |
        | command SP
        v
[Kết quả hợp lệ / trạng thái legacy được giữ nguyên]
~~~

---

## 5. Biểu đồ thiết kế

### 5.1. Sequence Diagram

~~~mermaid
sequenceDiagram
    autonumber
    actor User
    participant UI as React QC-05
    participant API as .NET API
    participant SP as SQL Contract
    participant DB as MMS Legacy Tables
    User->>UI: Thực hiện Tra cứu và hiệu chỉnh lịch sử QC
    UI->>API: Request đã validate sơ bộ
    API->>SP: UserId + contract input
    SP->>DB: Kiểm tra quyền và business rules
    alt Hợp lệ
        SP->>DB: Transaction/query nghiệp vụ
        DB-->>SP: Kết quả
        SP-->>API: Result set ổn định
        API-->>UI: 2xx
        UI-->>User: Hiển thị kết quả
    else Không hợp lệ
        SP-->>API: THROW 510xx
        API-->>UI: Problem Details
        UI-->>User: Thông báo + traceId
    end
~~~

### 5.2. Business Flow

~~~mermaid
flowchart TD
    S0["Lọc phiếu"]
    S1["mở chi tiết"]
    S2["cập nhật trường được phép"]
    S3["lưu audit."]
    S0 --> S1
    S1 --> S2
    S2 --> S3
    S3 --> V{"Hợp lệ?"}
    V -- Có --> OK["Hoàn tất QC-05"]
    V -- Không --> ERR["Từ chối và trả lỗi có kiểm soát"]
~~~

### 5.3. Architecture Flow

~~~mermaid
flowchart LR
    UI["React route"] --> API[".NET endpoint"]
    API --> SP["api.usp_* versioned"]
    SP --> ACCESS["User screen access"]
    SP --> DATA["Legacy tables/views"]
    DATA --> SP --> API --> UI
~~~

---

## 6. Acceptance Criteria và UAT

| Mã | Kịch bản | Kết quả mong đợi |
| --- | --- | --- |
| AC-QC-05-01 | User có quyền mở use case | Màn hình và dữ liệu đúng phạm vi được hiển thị |
| AC-QC-05-02 | User không có quyền | HTTP 403, không lộ dữ liệu |
| AC-QC-05-03 | Dữ liệu hợp lệ | Hoàn tất đúng luồng chính |
| AC-QC-05-04 | Thiếu dữ liệu bắt buộc | HTTP 400/422, chỉ rõ lỗi |
| AC-QC-05-05 | Đối tượng không tồn tại | HTTP 404, không ghi dở dang |
| AC-QC-05-06 | Dữ liệu đã thay đổi đồng thời | HTTP 409 hoặc kết quả nhất quán |
| AC-QC-05-07 | Lỗi hệ thống | HTTP 500 với 'traceId' |
| AC-QC-05-08 | Kiểm tra audit | User, thời gian và hành động đúng contract |
| AC-QC-05-09 | Kiểm tra phân quyền dữ liệu | Không thấy dữ liệu ngoài phạm vi |
| AC-QC-05-10 | Kiểm tra Power Apps dự phòng | Bảng và trạng thái legacy vẫn tương thích |

---

## 7. Cutover và dự phòng Power Apps

- React là giao diện chính sau cutover use case.
- Power Apps chỉ dùng dự phòng, không ghi đồng thời cùng use case React.
- Rollback bằng feature flag/route; không đảo ngược giao dịch đã commit.
- Trước khi bật Power Apps, dừng command React đang chạy và đối soát giao dịch cuối.

---

## 8. Traceability

| Nguồn | Tham chiếu |
| --- | --- |
| Hồ sơ nghiệp vụ | 'HO_SO_TONG_THE_UNG_DUNG_MMS.md' – QC-05 |
| Route registry | 'apps/web/src/app/routeRegistry.ts' |
| API source | apps/api/Modules/Quality/QualityEndpoints.cs |
| SQL source | database/stored-procedures/w2/api.usp_QC_QC05_GetInspectionHistory_v1.sql, database/stored-procedures/w2/api.usp_QC_QC05_UpdateInspectionResult_v1.sql |
| UAT chung | 'UAT_CUTOVER_CHECKLIST_MMS.md' |
| Kế hoạch | 'KE_HOACH_CHUYEN_DOI_POWER_APPS_SANG_REACT_MMS.md' |

---

## 9. Definition of Done

- [ ] React/API/SQL contract khớp kiểu dữ liệu và trường kết quả.
- [ ] Quyền màn hình và quyền dữ liệu được kiểm thử.
- [ ] AC-01 đến AC-10 đạt trên UAT.
- [ ] Không có lỗi 500 do thiếu hoặc sai object SQL.
- [ ] Audit và 'traceId' hoạt động.
- [ ] Đối soát xác nhận không thay đổi ngoài phạm vi use case.
- [ ] Nghiệp vụ và IT vận hành phê duyệt.
